# --- General Settings ---
# URL ที่จะเข้าใช้งาน DefectDojo (สำคัญมาก)
DD_SITE_HOSTNAME=defectdojo.your-domain.com

# Secret Key ของ Django (ต้องตั้งค่ายาวๆ สุ่มๆ)
# คุณสามารถสร้างได้โดยใช้คำสั่ง: openssl rand -base64 48
DD_SECRET_KEY=YourSuperLongAndRandomSecretKeyGoesHere

# --- Database Settings (PostgreSQL) ---
# ใช้ค่าเหล่านี้ใน docker-compose.yml
POSTGRES_USER=defectdojo
POSTGRES_DB=defectdojo
# รหัสผ่านสำหรับฐานข้อมูล (ตั้งค่าให้ปลอดภัย)
POSTGRES_PASSWORD=YourStrongDatabasePassword

# --- DefectDojo Admin User (สำหรับสร้างตอนแรก) ---
# ข้อมูลแอดมินที่จะสร้างเมื่อระบบเริ่มทำงานครั้งแรก
DD_ADMIN_USERNAME=admin
DD_ADMIN_PASSWORD=YourStrongAdminPassword123!
DD_ADMIN_EMAIL=admin@your-domain.com

# --- Celery/Redis Broker Settings ---
# ใช้สำหรับระบบ Task Queue (ไม่ต้องเปลี่ยนก็ได้ถ้าใช้ตาม compose นี้)
DD_CELERY_BROKER_SCHEME=redis
DD_CELERY_BROKER_USER=
DD_CELERY_BROKER_PASSWORD=
DD_CELERY_BROKER_HOST=redis
DD_CELERY_BROKER_PORT=6379
DD_CELERY_BROKER_VHOST=0

# --- Timezone Settings ---
# ตั้งค่า Timezone (ตัวอย่าง: Asia/Bangkok)
TZ=Asia/Bangkok
